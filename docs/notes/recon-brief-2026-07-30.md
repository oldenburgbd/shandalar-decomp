{
  "summary": "Multi-front reconnaissance for a byte-accurate RE project on MicroProse Magic: The Gathering (1997) + ManaLink",
  "agentCount": 30,
  "logs": [
    "Recon complete: 5/5 lanes returned",
    "78 claims to verify",
    "39 high-confidence critical claims -> adversarial verification",
    "Verification: 15 survived, 9 refuted"
  ],
  "result": {
    "brief": "# MTG (MicroProse, 1997) — Shandalar / ManaLink: Byte-Matching Decompilation Recon Brief\n\n**Date:** 2026-07-30 · **Target install:** `C:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA` · **15 PE modules** (two distinct Facemaker builds)\n\n---\n\n## 1. Verdict on feasibility\n\n**Near-100% byte-for-byte across the target set is not realistic. Near-100% *per-function* byte equality after relocation normalization is realistic for the two big EXEs, and whole-file SHA-256 identity is realistic only for a handful of small modules.** Restate the goal in these terms before committing budget.\n\nDefine the ladder explicitly and gate on it:\n\n| Level | Meaning | Verdict |\n|---|---|---|\n| **L0** | Same instruction stream, addresses/immediates may differ | **Achievable.** This is what isle actually shipped. |\n| **L1** | Per-function byte equality after relocation/address normalization | **Achievable for the large majority.** Realistic ceiling ~90–97% of functions; a stubborn tail remains. |\n| **L2** | Per-TU relocatable COFF `.obj` equality | Achievable but gated on unproven PE-splitting tooling (see §4). |\n| **L3** | Whole `.text` / `.data` / `.idata` / `.rsrc` byte-identical | Achievable per-section only after link order and data layout are frozen. |\n| **L4** | Whole-file SHA-256 equality | **Realistic for Statwin/Magsnd/Magvid/Manalinkinterface-class modules. Not defensible for Shandalar.exe or Magic.exe.** |\n\n**Why the ceiling exists — four concrete, evidenced reasons:**\n\n1. **MSVC 4.2 codegen entropy.** isle's current README names this as their one unsolved blocker: unrelated header edits pseudo-randomly change codegen in the same TU, \"roughly affects around ~5% of all decompiled functions,\" cause unknown after 38 months and 41 contributors. Same compiler, same problem here, no known fix.\n2. **isle's actual result.** LEGO1.DLL 100.00% implemented / **99.61% accuracy**; ISLE.EXE 100.00% / **99.40%**; CONFIG.EXE 100.00% / **99.72%**. README states plainly the binaries \"are not a byte-for-byte match.\" 18 months to 100%-implemented, still short of byte-for-byte at 38 months. That is the honest calibration point.\n3. **Non-linker-reproducible bytes exist in the target set.** Deck.exe carries a 45-byte XOR-0x4F post-link build stamp at file offset 0x4000 decoding to `\" September 2, 1997: Release Build\"` — appended by a proprietary MicroProse tool five months after the link timestamp. It is the only module in the install with a file overlay. Magic.exe carries Mok's 2001 `.mok` section.\n4. **No `.reloc` on the EXEs** (`Characteristics 0x10F`). Every global address is baked into `.text` as an absolute immediate, so any `.data` layout error invalidates already-matched code wholesale.\n\n**Two structural facts that make it *easier* than feared:** every module has `OptionalHeader.CheckSum = 0` (no checksum reproduction needed), and the big EXEs have zero overlay with all-zero header padding (Shandalar `SizeOfHeaders=0x400`, pad `0x268..0x400` all zero, last raw section ends exactly at filesize `0x1AC000`).\n\n**Mok's `.mok` patch is a solved, trivial problem — not a blocker.** It is four in-place 6-byte `push imm32; ret` overwrites (24 bytes destroyed total) plus one appended RWX section (443 non-zero bytes, span `0x0095A000–0x0095A20F`). **All 24 destroyed bytes are known**, and the bypassed original `.text` at all four sites is intact and directly decompilable:\n\n| Site (VA) | Original bytes | Reconstruction basis |\n|---|---|---|\n| `0x0055DCD0` (PE EP) | `64 A1 00 00 00 00` (`mov eax, fs:[0]`) | Shandalar `0x0057E970` + Manalink `0x004214A0` identical; `.mok+0x28` re-executes it |\n| `0x004B59FA` | `C7 45 FC F4 01 00 00` (`mov [ebp-4], 500`) | Byte-identical unpatched twin at Shandalar `0x0056C1FC` |\n| `0x0050D2D8` | `6A 00 FF15 F80A9500 48` (`push SM_CXSCREEN; call GetSystemMetrics; dec eax`) | Leftover tail `95 00 48` + parallel block at `0x0050D2E7` |\n| `0x00552E4D` | `6A 01 FF15 F80A9500` (`push SM_CYSCREEN; call GetSystemMetrics`) | Leftover `95 00` + parallel block at `0x00552E56` |\n\nThe PE header was **not** repointed — `AddressOfEntryPoint` was always `0x15DCD0`. Mok's only header edits: `NumberOfSections` 6→7, `SizeOfImage` 0x55A000→0x55B000, one appended 40-byte header, 0x1000 appended raw bytes.\n\n---\n\n## 2. The toolchain question\n\n**This risk is closed. Visual C++ 4.2 is confirmed as the toolchain and is confirmed running natively on this machine.**\n\n### Identification — proven, not inferred\n\n- `LINK.EXE` self-reports **`Microsoft (R) 32-Bit Incremental Linker Version 4.20.6164`**; `C2.EXE` reports `Version 10.20`; `_MSC_VER == 1020` (verified with a live `#error` negative control). `_MSC_VER` alone pins 4.2 (4.0=1000, 4.1=1010, 4.2=1020) — **do not cite MS KB Q164951, it is DEC Alpha/MIPS/PowerPC-only and says nothing about x86.**\n- **Strongest evidence: 38,963 bytes of MSVC 4.2 `LIBCMTD.LIB` code appear verbatim in Drawcardlib.dll** (38,192 B in Cardartlib.dll, 23,757 B in Cdtools.dll), with contiguous exact runs up to 763 bytes. Control: Gcconn.dll (MSVC 5.0) scores 1,898 B / 1.0%.\n- Corroborating: **342/342 MSVCRT import hints match** across all 8 dynamically-linked modules, zero mismatches. This pins a 754-name `msvcrt.dll` export snapshot — excluding the shipped VC6-era `Msvcrt.dll` (759 names, 86/89 hints differ) and Win11's (1440 names, 13/13 differ). It does *not* on its own exclude MSVC 5.0; the PE linker stamp 4.20 does that.\n\n### Runs on Windows 11 x64 — verified end to end\n\nNo DOSBox, no Wine, no VM. Requirements: (a) `C:\\Windows\\SysWOW64\\msvcrt40.dll` (60,928 B, already present); (b) `MSPDB41.DLL` must sit alongside `CL.EXE`/`LINK.EXE` — proven by removal (exit `0xC0000135`, no output). Compile + link + execute + PDB generation all verified.\n\n**Path-with-spaces is a non-issue.** isle's README warns against it, but this was tested here: full compile+link+run succeeded from `C:\\vc42 space test\\deep sub dir\\` with a source file named `sp test.c`. **Do not restructure the repo for this.** Using a short space-free path (`C:\\mtg`) remains cheap insurance for path-length limits, nothing more.\n\n### Primary plan — acquisition\n\n1. **Buy genuine VC 4.2 media** (2-CD Professional/Enterprise, confirmed available secondhand on eBay for very little). Extract `BIN\\`, `INCLUDE\\`, `LIB\\` yourself. Cleanest legal posture, and cheap.\n2. **Verify your copy** against the known-good build every reference project uses: `C2.EXE` = 549,888 bytes, MD5 `dcd69f1dd28b02dd03dd7ed02984299a`, SHA-256 `674fb9e410481378c6980c3f21914e513128c85001e8337aca73b587b6273ae9`; `LINK.EXE` reports `4.20.6164`. Acceptance test for *other* media should be looser: **LINK 4.20.x AND `_MSC_VER == 1020`** — the PE header stores only `4.20` with no build number, and 4.20.6191 also exists (VC 4.2b Alpha Edition).\n3. **Vendor a hashed local copy immediately.** `itsmattkc/MSVC420` is an unlicensed re-host with no LICENSE file, last pushed 2021-11-02; it can be DMCA'd mid-project. Mirror exists at `madebr/MSVC420` (independent re-upload, no common ancestor, pushed 2025-10-12).\n4. **Apply isle's C2 patch:** `tools/patch_c2.py` NOPs two 5-byte `E8` calls at file offsets `0x52F07` and `0x74832` to suppress warning C4786. Only needed for C++ with long identifiers under warnings-as-errors; harmless otherwise.\n\n**What is currently on disk and what is missing.** Working 5-file toolchain exists at `...\\scratchpad\\vc42test\\` (CL, C1, C2, LINK, MSPDB41) plus `LIBCD.LIB`, `LIBCMTD.LIB`, and a second copy at `C:\\vc42verify`. **Move these somewhere durable.** Still missing and required: `NMAKE.EXE`, `LIB.EXE`, `RC.EXE`, `CVTRES.EXE`, `VCVARS32.BAT`, the full `include/` tree (392 entries), the full `lib/` tree (115 files), and **`LIBC.LIB`/`LIBCMT.LIB` (release static CRT)** — needed to close out Manalink.exe.\n\n### Plan B — named\n\n**Plan B is not a different compiler. It is decomp.me plus a downgraded goal.**\n\n- **decomp.me** hosts the exact compiler: platform `win32`, compiler id `msvc4.2`, **4,287 existing win32 scratches**. Verified functionally on 2026-07-30 — scratch `https://decomp.me/scratch/ssXus` compiled under `msvc4.2 /O2 /Gd` with a perfect match (score 0/300). `<windows.h>`, `<mmsystem.h>`, `<string.h>` all compile. Caveats: no auto-decompiler (`has_decompiler:false` — Ghidra output must be pasted manually), targets are pasted as Intel-syntax assembly text (you cannot upload a PE or `.obj`), flags are fixed flagsets not free-form, and all scratches are world-readable.\n- **If genuine 4.2 media proves unobtainable and the mirrors vanish:** restate the goal as **L0 semantic/instruction-level equivalence with a modern toolchain**, and reposition the **Ghidra annotation database as the primary deliverable** (it would be the first ever published for these binaries — see §4). MSVC 14.51 cannot produce a 10-entry data directory, cannot omit the Rich header, and will not reproduce 1996 register allocation; L1 is off the table without 4.2.\n\n### Gcconn.dll — a separate toolchain, recommend descope\n\nLinker 5.00 = MSVC 5.0 pre-SP3 (SP3 raises the VC5 linker to 5.10). No portable drop exists (itsmattkc publishes 400/420/600 only; decomp.me jumps 4.2→6.0). It *is* sourceable — VC++ 5.0 Professional is a free 254 MB ISO on WinWorld — but descope it on **provenance and reachability** grounds, not toolchain grounds: it is third-party TEN SDK middleware (`tenutils.cpp`, `ctenthread.cpp`, `TenSimpleChan`, RTTI `.?AVtenDbCVerifyException@@`) with **no `D:\\NewMagic` paths at all**, only Manalink.exe links it (12 of 46 exports), Shandalar.exe and Magic.exe never touch it, and it services an online network dead since ~1999. Stub or document the 12 imported entry points.\n\n**The toolchain gap that actually matters is `.mok`**, which needs an assembler and sits inside an in-scope module. Budget for that; it is small (443 bytes) and fully characterized above.\n\n---\n\n## 3. Scope — real numbers\n\n### The two EXEs\n\n| | Shandalar.exe | Magic.exe |\n|---|---|---|\n| File size | 1,753,088 B | 1,604,096 B |\n| Functions (Ghidra) | 2,234 | 1,973 |\n| `.text` bytes | 1,544,513 | 1,416,359 |\n| `.data` VirtualSize / RawSize | `0x406DB0` (4.2 MB) / `0x25E00` (155 KB) | `0x3F07A0` / `0x20800` |\n| `.rdata` | `0x2EA` (746 B) | — |\n| `.reloc` | none (`Char 0x10F`) | none (`Char 0x10F`) |\n| PE timestamp | 1998-03-24 16:38:05Z | 1998-03-24 16:25:52Z |\n\n**Twelve minutes apart in the same build session.** They are not two independent programs, and they are not one program with two shells.\n\n### Code relationship — the number that drives the plan\n\nMeasured over Shandalar's 1,544,513 `.text` bytes, with image-range immediates and rel32 branch targets normalized:\n\n| Bucket | Functions | Bytes | % of Shandalar `.text` |\n|---|---|---|---|\n| **A — identical codegen** | 981 | 374,237 | **24.2%** |\n| **B — matched, <10% instrs differ** | 454 | 627,206 | **40.6%** |\n| **C — matched, ≥10% instrs differ** | 271 | 272,147 | **17.6%** |\n| **D — Shandalar-only** | 504 | 270,923 | **17.5%** |\n| Magic-only | 236 | 143,418 | 10.1% *of Magic* |\n\n**Source-level shared ≈ 84% ± 2 points** (bounded [82.3%, 86.0%]; Magic side [89.8%, 93.8%]). **Object-level identical is only 24.2%.** Union of distinct source functions: **~2,350 functions / ~1.66 MB** (bounded [1.633, 1.689] MB).\n\n**Consequence — this is the single most important planning fact in the brief: you cannot build one shared static library, link it into both executables, and get a byte match.** Structure the *sources* as shared core + two shells, but expect **two separate compilations with independent per-target verification**. Roughly 70% of the \"shared\" bytes need byte-matching work twice.\n\n**Only 1 of 725 near-pairs has its divergence confined to a trailing region; 444 of 724 first diverge within the first quarter of the function.** The divergence is interior and real, not a jump-table or padding artifact.\n\n### The engine is forked, not shared\n\nFrom the binaries' own `__FILE__` strings:\n\n- **sidlib is FORKED.** Shandalar links `D:\\NewMagic\\sources\\sidlib\\{lib,text,sprite,Fileio,Pcxw}.c`. Magic links **only** `D:\\Newmagic\\multiplayer\\sidlib\\Pcxw.c`. The 94 Shandalar-only functions in band `0x570000` (22.6 KB) have best-case Jaccard **0.00–0.04** against anything in Magic.exe — genuinely absent, not mis-matched. Shandalar ships an assert-enabled sidlib with a debug palette-viewer window (`\"WM_CREATE CreateDIBSection\"`, `\"Current Palette\"`, `\"ShowPaletteClass\"`); Magic does not.\n- **NedCard is forked/divergent.** Magic.exe contains *two* build-root spellings of `Catalog.c`. The `Catalog.c` pair (Shandalar `0x0042D9E0` / Magic `0x0040CA30`) differs in real codegen — `xor eax,eax; cmp [ebp-8],-1; setne al` vs `mov eax,[ebp-8]; inc eax; cmp eax,1; sbb eax,eax`. Only `Palette.c` and one `haar.c` function are truly identical.\n- **Link order differs between the EXEs.** Shandalar: Catalog(42d) Palette(42f) Ai(4c3) iniFile(4c8) Test(4cd) glue(559) haar(566) Minit(56c) lib(579) text(57a) sprite(57b) Fileio(57c). Magic: Catalog(40c) haar(41f) glue(4a7) Palette(4ba) Ai(4e4). 6,394 distinct match displacements — no global shift.\n\n**Corrected region atlas:** `0x570000` = **sidlib** (not \"cue-card rendering\"). `0x450000` = **Magic's shell/menu system** (76.2% Magic-only, 39 fn / 49 KB — the largest Magic-only concentration; this is where `SHELLPAGE_*`/`SHELLSCREEN_*` actually live). `0x4A0000` = **Shandalar's own shell** (33.9% Shandalar-only: `openingMenu`, `menubak.pic`, `diffCaption`, `colorMenuColors`).\n\n### CRT / library code that does *not* need hand-decompilation\n\n**In the two EXEs: essentially none.** Both link `/MD` against MSVCRT.dll (89 and 71 imports). Static compiler-support bucket:\n\n- Shandalar: **693 bytes / 8 functions / 0.044%** — `_chkstk` `0x0057E860` (47 B, 11 callers), onexit/atexit registration shim `0x0057E890` (156 B), `atexit` `0x0057E930` (19 B), `__allshl` `0x0057E950` (31 B), `WinMainCRTStartup` `0x0057E970` (**418 B**, not 378), `_setdefaultprecision` `0x0057EB30` (19 B), `_matherr` stub `0x0057EB50` (2 B), empty `ret` `0x0057EB60` (1 B). Plus 15 six-byte import thunks = 783 B total.\n- Magic: **518 bytes / 0.036%** + 14 thunks = 602 B.\n- **Whole-`.text` signature scan for `__allmul`, `__alldiv`, `__aulldiv`, `__allrem`, `__aullrem`, `__allshr`, `__aullshr`, static `__ftol`, `__EH_prolog`: ZERO hits.** `__allshl` is the only helper present. A tail-only enumeration did not miss scattered helpers.\n\n**Outside the two EXEs, static CRT is substantial and must be planned for (~250–350 KB):**\n\n| Module | CRT model | CRT burden |\n|---|---|---|\n| Manalink.exe | **Static release `/MT` (LIBCMT.LIB)**, VC 4.2 | ~44,662 B ≈ **24.8% of `.text`**. EP `0x004214A0` is static crt0 calling internal `_heap_init`/`_mtinit`/`_ioinit`/`_setargv`/`_setenvp`/`_cinit` |\n| Manalinkinterface.dll | Static release `/MT`, VC 4.2 | `_mtinit` at `0x10002BE0`, `_mtterm` at `0x10002C40` |\n| Drawcardlib / Cardartlib / Cdtools | **Static debug `/MTd` (LIBCMTD.LIB)**, VC 4.2 | CRT-string spans **57.8% / 65.2% / 91.7%** of `.text` |\n| Gcconn.dll | Static debug `/MTd`, **MSVC 5.0** | 54.3% span; zero match against VC 4.2 libs |\n| Shandalar/Magic/Deck/Facemaker/Deckdll/Magsnd/Magvid/Statwin | `/MD` MSVCRT.dll, VC 4.2 | negligible |\n\n**Do not use CRT source-filename greps to distinguish ST from MT.** `LIBCD.LIB` and `LIBCMTD.LIB` have identical 614-member basename lists; `mlock.c`/`tidtable.c` appear in both (and in the dynamic `Msvcrtd.dll`). Valid discriminators: absence of a CRT DLL import (= static), `TlsAlloc`/`GetCurrentThreadId` imports (= MT), and direct byte-matching of extracted lib objects. The shipped `Msvcrtd.dll` is imported by **no** module.\n\n### Bottom line: functions requiring human/agent work\n\n**~2,330 functions for the two EXEs** (2,350 union − ~10 CRT/compiler-support − 8 MPS_CODE blocks, shared once). Plus per-module work for Deckdll (269,824 B), Drawcardlib (178,176), Cardartlib (139,264), Manalink.exe (306,176), Facemaker (76,288 / 89,088), Cdtools (75,264), Statwin (55,808), Manalinkinterface (46,080), Magvid (45,056), Magsnd (25,088), Deck.exe (**one** 114-byte function).\n\n**Translation-unit count (medium confidence):** ~262 sixteen-byte-aligned `0xCC` runs in Shandalar and ~170 in Magic across 2,234 / 1,973 functions ⇒ **~280 and ~185 TUs**, ~8 functions and ~5.6 KB per TU. Functions are packed with no per-function alignment, so these gaps are object-file contribution boundaries. *Not yet confirmed against a known symbol — cross-check against the 15 recovered `__FILE__` anchor addresses.*\n\n### Mega-function warning\n\nLargest single `.text` block: **99,392 B in Shandalar (6.4% of `.text`), 106,592 B in Magic (7.5%)**. Blocks >4 KB are 34% of blocks but **91.5% of bytes**. Mean function size 700/724 B — 3–5× typical 1990s C. **Byte-weighted and function-weighted progress will diverge violently.** Spike one mega-function early to learn MSVC 4.2's switch-lowering and jump-table emission, then template it.\n\n---\n\n## 4. Prior art to reuse\n\n### The single largest body of prior art\n\n**`https://github.com/ShandalarMagic/Shandalar`** (94 stars, 52,332 files, **only 2 commits, both 2021-09-03 — a frozen snapshot, not a live mirror; do not expect responsive upstream**). Clone it in full before it disappears.\n\n- **`src/defs.h` (2,975 lines)** — `card_instance_t` (~0x12C bytes, field-by-field with offset comments and \"untouched by exe\" annotations), `card_data_t` (72 B, STATIC_ASSERT-guarded), `stack_data_t`, `target_t`, `card_txt_t`, ~56 enums (`event_t`, `trigger_t`, `keyword_t`, `type_t`, `phase_t`, `status_t`, `counter_t`, `special_flags1/2/3_t`). Companions: `manalink.h` (2,139), `subtypes.h` (648), `stypes.h` (342), `Expansions.h` (159). `#ifdef SHANDALAR` blocks prove they RE'd Shandalar.exe too. **Adopt as the Ghidra type seed — this removes the single largest time sink.**\n- **`src/manalink.lds`** — 313 named symbols (~171 functions, ~142 globals): `_damage_player=0x4A68A0`, `_draw_cards_exe=0x41C190`, `_life=0x4EF528`, `_mana_pool=0x4EF4E0`, `_stack_data=0x509630`, `_cards_data=0x7E7010`. **⚠ NAMES ONLY — the addresses are for ManaLink 3.0's own rebuilt Magic.exe** (3,186,688 B, md5 `6a6672769238436b6701688bd0ede350`, custom `.map`/`.cards` sections). Prologue-byte hit rate: **88% on ManaLink's Magic.exe vs 1% on your Magic.exe and 1% on your Shandalar.exe.** Budget explicit signature-matching effort; use ManaLink's Magic.exe as the bridge.\n- **`src/patches/*.pl` (59 Perl scripts)** — annotated original disassembly listings with exact VA/file-offset pairs. `Patch.pm` bases: magic.exe `0x400000`, shandalar.exe `0x400c00`, manalinkex.dll `0x1000c00`.\n- **⚠ False lead:** `MAGIC3.map … MAGICd.map` are **not linker maps** — they are 320×200 `X0`-format PIC images (Shandalar overworld maps).\n- **⚠ License unverified.** Matters if you copy struct definitions verbatim.\n\n### Verification and build tooling\n\n- **`https://github.com/isledecomp/reccmp`** — the verification substrate. Explicitly targets \"C++ compiled to 32-bit x86 with old versions of MSVC (like 4.20)\". `pip install reccmp` (current 0.1.6, **pre-1.0, pin it**). Ships `cvdump.exe` (979,968 B) inside the wheel — no separate Microsoft download for PDB parsing. Needs only *your* build's PDB, never the original's — correct for this project, where no original PDBs survive. Tools: `reccmp-reccmp`, `-project`, `-decomplint`, `-verexp`, `-vtable`, `-datacmp`, `-stackcmp`, `-roadmap`, `-aggregate`, plus a Ghidra importer.\n  - ⚠ **Scope caveat:** README's next sentence says \"Other compilers, **languages** and architectures are not supported.\" This codebase is C. The asm-diff core is language-agnostic and works fine, but `reccmp-vtable` is inapplicable (no vtables in C) — budget ~8 of 11 tools as useful.\n  - ⚠ **`reccmp-verexp` is not a day-one check.** It reads both `original_path` *and* `recompiled_path` — it requires a DLL that already builds, links, and exports. For day-one export recon, diff the PE export directories yourself.\n- **`https://github.com/isledecomp/isle`** — the build recipe. `tools/patch_c2.py` (verified byte-for-byte). Flags from `CMakeLists.txt` lines **584–659**: CXX base `/W3 /GX /D WIN32 /D _WINDOWS`; RelWithDebInfo `/Zi /O2 /D NDEBUG`; Debug `/Gm /Zi /Od /D _DEBUG`; Release `/O2 /D NDEBUG`; MinSizeRel `/Os /D NDEBUG`; link `/machine:I386`, RelWithDebInfo `/incremental:no /debug`, per-final-binary `/OPT:REF`. `JOB_POOLS` forced to 1 (no parallel builds). \"nmake must be run twice\" is from **README.md**, not CMakeLists.\n  - ⚠ **isle defines NO `CMAKE_C_FLAGS` at all** — only `CMAKE_CXX_FLAGS*`. For our C targets the whole block must be re-authored as `CMAKE_C_FLAGS_*` or CMake silently substitutes its own defaults.\n  - ⚠ `pip install -r tools/requirements.txt` pulls reccmp from an **unpinned** `git+https://github.com/isledecomp/reccmp`. Pin to a commit SHA on day one.\n  - ⚠ isle's master-baseline diff ends in `|| echo \"Current master not found\"` — it **reports** regressions but never fails CI. Add a real gate yourself.\n  - **Reuse reccmp, not isle's `build.yml`.** That workflow is project-specific glue (hardcoded `CONFIG`/`ISLE`/`LEGO1`, legoisland.org asset fetch). Use `reccmp-project create` and author your own `reccmp-project.yml`.\n- **`https://github.com/itsmattkc/MSVC420`** (30.7 MB, 63 stars, no LICENSE, pushed 2021-11-02) + mirror **`https://github.com/madebr/MSVC420`** (pushed 2025-10-12, adds CMake/CLion vcvars snippets). `bin/` is a near-complete MSDEV tree, not just CLI tools. `lib/` 115 files, `include/` 392 entries — build-complete including LIBCD/LIBCMTD.\n- **`https://decomp.me`** — platform `win32`, compiler `msvc4.2`. Verified working (§2).\n- **`https://winworldpc.com/product/visual-c/4x`** (ISOs) · **`https://archive.org/details/en_vc42ent`** (⚠ **two self-extracting installers**, `en_vc42ent_disc1.exe` 193.4 MB + `disc2.exe` 27.5 MB — *not* ISOs).\n\n### Same-binaries active scene (2026)\n\n- **`https://github.com/rlerrr/shandalar-patch`** — **highest-value coordination target.** Active Mar–Apr 2026, same binaries, byte level. Publishes `specs.json` (size + MD5 for all 14 official ManaLink 1.3 US binaries) and `pe_patcher.py`. Also publishes the real DRM call sites (Win95/98 registry ProductId checks, *not* MPS_CODE): deckdll.dll `0x1000DEB9` & `0x100271E3`, magic.exe `0x0043D29B` & `0x00497A09`, shandalar.exe `0x00441BF9` & `0x00468498` (⚠ VAs are for canonical 1.3, not your binaries).\n- `https://github.com/jennifer-mortensen/Shandalar-Tools` (Apr–May 2026, Python card_loader/card_processor)\n- `https://github.com/mirkwoodrunner/shandalar-modern` (commits daily through 2026-07-30; JS from-scratch reimplementation, itself Claude-assisted — consumes findings, produces nothing byte-level)\n- `https://github.com/ogamespec/ShandalarToolbox` (touched 2026-05-18), `ogamespec/WvlDecoder`\n- **`https://slightlymagic.net`** — the primary community archive. Forums f=25 (ManaLink 3.0), f=76 (Shandalar), f=74 (MicroProse MTG), f=85 (patches). Threads t=7509 (PicViewer/image tools), t=22306 (editing .pic), t=17355 (SVE save editor), t=28874 (SPR format). **Unreachable from the sandbox (ECONNRESET on every fetch) — browse manually. This is an unmined seam.**\n- `https://archive.org/details/score54cd` contains `mtg_13us.zip` (canonical ManaLink 1.3 US set with published hashes).\n\n### File-format oracles (differential test harnesses)\n\n`https://github.com/CelestialAmber/ShandalarImageToolbox` — `PicDecoder.cs`, `CatFile.cs`, `Wavelet.cs`, `Vlc.cs`, `SprDecoder.cs`, `PalDecoder.cs`. Plus `benprew/mp_pic_tools`, `ogamespec/PicDecoder`, Quadko's `PicFileFormat.txt`. **Use these to validate MPS_CODE's decompilation; do not re-derive the formats.**\n\n### Confirmed negatives\n\n- **No Ghidra project, IDA database, or annotated disassembly has ever been published for the stock binaries.** Your annotation DB would be the first of its kind and is worth publishing as a standalone deliverable.\n- **No LLM-assisted decompilation exists.** The one forum thread (slightlymagic f=76 t=31995, Mar/Apr 2024) is two speculative posts with no work product, no repo, no follow-up. Nothing to be scooped by; also a mild warning that the community floated this and nobody delivered.\n- **`decomp-permuter` has no x86 backend.** README: \"supports MIPS (compiled by IDO, possibly GCC), PowerPC, and ARM32 assembly.\" The related `asm-differ` has only \"limited\" x86 support. **This is the single biggest tooling gap** — the last 5–10% of functions in every matching decomp is closed by permuter-style search, and it does not exist here. Porting pattern: fork it (cf. `DTeachs/decomp-permuter-arm`). The randomizer half is architecture-neutral C-source mutation; only the scorer and compile-invocation layer are arch-specific. **~150–300 agent-hours.**\n- **`objdiff` is NOT ecosystem-incompatible — the earlier \"poor fit\" assessment was wrong.** `objdiff-core/src/arch/mod.rs` maps `object::Architecture::I386`/`X86_64` → `x86::ArchX86`; `arch/x86.rs` is a first-class backend; `obj/read.rs` branches on `BinaryFormat::Coff` and parses COFF line-number auxiliary records. objdiff's x86 support was historically **COFF-only — purpose-built for MSVC `.obj`**. The real cost is the **splitting** step (carving reference `.obj`s out of a whole PE), which needs the **openblack fork** of decomp-toolkit — unsupported, undocumented for third parties. `openblack/bw1-decomp` (Black & White 1, MSVC 6.0 SP4/SP5) is the **only** confirmed x86-PE objdiff precedent and sits at ~1.75% decompiled after substantial effort. **Defer objdiff on splitting-cost grounds; keep it on the roadmap as the endgame byte-identity verifier.** (⚠ `CMR2Decomp` is a **reccmp** precedent, *not* an objdiff user — its README credits reccmp only.)\n- **`objdiff`'s report protobuf** (`objdiff-core/protos/report.proto`) is a mature progress data model already ingested by `https://decomp.dev`. Adopt the schema verbatim.\n- **`boricj/ghidra-delinker-extension`** supports COFF/x86 export. *Medium confidence* — its ELF/MIPS path is far more exercised. Spike on Deck.exe before depending on it.\n\n---\n\n## 5. Recommended methodology\n\n### 5.1 Correct the standing project assumptions first\n\nThree things in the current brief are wrong and will misdirect work:\n\n1. **`MPS_CODE` is NOT anti-piracy.** It is MicroProse's hand-written assembly **PIC image codec** (LZW + RLE90). At Shandalar VA `0x0098A000` / Magic `0x00953000` / Drawcardlib `0x10155000` / Deckdll `0x101F7000`. Header dispatch at `+0x43`: `cmp ax,0x304D` (\"M0\"), `cmp ax,0x314D` (\"M1\"). It uses ESP as the LZW string stack (`xchg dword ptr [0x5A58CC], esp` at `0x0098A318`/`0x0098A38B`, stack base `0x005A5CEB`) — an unmistakable hand-assembly idiom, which is why it sits in its own segment.\n   - **⚠ Branch polarity: it decodes `.pic` ONLY, never `.pcx`.** Both containing functions do `strrchr(name,'.')` → `stricmp(\".pcx\", ext)` → `jne`. In `FUN_0057C580` the `jne` at `0x57C5AD` goes to `0x57C6C2`, which is where `_open` + `call 0x98A000` live. **MPS_CODE runs on the NOT-`.pcx` branch.** Real `.pcx` files take the other path (fopen + C row reader `FUN_0057DA00` + `FUN_00579F10`). Install ships 418 `.pic` and 8 real `.pcx`.\n   - **`'X'` is not a terminator** — it is the image-data chunk tag, and the high byte selects X0 (8bpp) vs X1 (4bpp packed): `cmp al,0x58` exits the chunk loop, then `and ah,1` stores the depth selector to `[0x5A58E5]`.\n   - **8 blocks / 378 instructions / all 1192 bytes.** Ghidra's \"6 functions / 1007 bytes\" undercounts. Public entries are `+0x000` (open/parse header), `+0x484` (decode one row), and `+0x18C` (alternate lightweight init — dead in Shandalar but **genuinely called at `0x0040B20B` in `Facemaker\\Facemaker.exe`**, so it must be reproduced). `+0x245`/`+0x2B5`/`+0x300`/`+0x392` are internal. `+0x1FC` and `+0x21E` are dead row-decode wrappers.\n   - **⚠ Preserve a latent bug:** `+0x1FC` is a near-twin of `+0x484` but omits the `push ebp`/`pop ebp` guard around `call 0x98A300`; since `+0x392` does `pop ebp`, `+0x1FC`'s `leave` restores ESP from garbage. **A matching decomp must reproduce this.**\n   - **Identical across 6 modules** (Shandalar.exe, Magic.exe, Deckdll.dll, Drawcardlib.dll, and both Facemaker.exe builds) modulo **115 `IMAGE_REL_BASED_HIGHLOW` relocation slots + one rel32 CALL displacement at offset 215** (not \"116 relocations\"; the length-3 run is because the rel32's high byte is always `0xFF`). Masked hash of the remaining 728 bytes: `3836eee0a8cd26ef…`. **Use as an automated build check.**\n   - **⚠ It is not self-contained.** The one outbound CALL at `MPS+0xD6` resolves to *different* code per module — an empty stub (`55 8B EC 53 56 57 5F 5E 5B C9 C3`) in Deckdll/Drawcardlib/Magic, a real `rep movsd` routine in both Facemakers and Shandalar. It references 22 data symbols (20 in `.data`, 2 in BSS), one of which is a **function pointer** driving 10 indirect calls that host code must populate at runtime. Inter-cluster `.data` gaps differ per module. It also needs C-side callbacks `FUN_0057A060` (palette install) and `FUN_0057C860` (refill), and RLE/LZW/ESP-stack state **persists across row-decode calls** — only `+0x245` resets it.\n   - **⚠ Emit it as assembled source, not a `#pragma data_seg` byte blob.** The four modules with `.reloc` need real linker fixups on those 115 slots, which a byte array cannot produce. *(Medium confidence on ml.exe/LINK 4.20 object compatibility — spike this.)*\n\n2. **CdTools is not a deferrable whole function.** In Shandalar all 9 CdTools call sites (`FF 15` only — no thunks, no `GetProcAddress` path) are at `0x0054500D`–`0x00545231`, inside the function at `0x00544F66`. But that function is **5,238 bytes** (to `0x005463DB`; Ghidra's 5,213 truncates the final block), and **only its first 758 bytes are the CD gate**. The other 86% is expansion/sealed-deck multiplayer duel logic (`_beginthreadex`, `SetThreadPriority`, `WaitForSingleObject`, `ReleaseMutex`, `DialogBoxParamA`; `DIALOG_ENDEXP1DUEL_*`, `MP_UIStrings.txt`, `SEALDECK_OPPONEDITING`) calling 17 internal functions. **Defer the ~760-byte prologue block, not the function.** And in Magic.exe the isolation property fails entirely: CdTools is called from *two* functions, and `FUN_004554B0` uses `IsCDDrive` to walk drives D..Z locating `\"Autoplay\\%s\"` — **asset path resolution, not protection.** It cannot be stubbed to a constant.\n\n3. **Refine, don't drop, the caller-count heuristic.** Ranked by unique callers, #3 overall is the MSVCRT `strcpy` thunk (197); by call sites `strcpy` is #1 with 974. Of the 29 functions with ≥50 callers, 4 are CRT thunks and 13 are ≤200 B helpers — exactly what the rule predicts. Replace the guidance with: *\"Resolve imports and thunks first. Among remaining unnamed functions, high caller count means **centrality**, not library-ness — but high caller count **combined with size ≤200 B and zero callees** still reliably indicates a helper/accessor.\"*\n   - Relabels: `0x0056CA10` (568 B, 361 callers) = `int LoadStringSection(const char *filename, const char *sectionTag)` — builds `\"@\"+tag+\"\\n\"`, `fopen(f,\"rt\")`, `fgets(300)`-scans to the marker, `fscanf(\"%d\",&count)`, loops `i < min(count,250)` into the global table at `0x008AA920` (300-byte stride). Drives `Advstrings.txt`/`Uistrings.txt`/`Prompts.txt`/`Menus.txt`/`Hints.txt`. **⚠ Contains dead code that must be reproduced to match:** the prefix built at `0x56CA57`–`0x56CA7C` (`strcpy(buf2, g_0078E5F0); strcat(buf2, \"\\\\\")`) is immediately clobbered by `strcpy(buf2, filename)` at `0x56CA8A`.\n   - `0x0046D8C1` (621 B, 310 callers) = state **mutator** (test bit `0x80`, early-out, OR it in, read field, tail-call `0x0046DBED`), not an accessor.\n   - `0x004BEA85` (9,019 B, 378 blocks, 192 callers) = card-text **builder** — 51 of 60 calls are `strcat` into `[ebp-0xCC]`. **Schedule LAST**: it consumes the struct layout, it does not teach it.\n\n4. **The card engine is not ~600 KB and is not anchored by per-card strings.** The `UPPER_SNAKE` strings are **Prompts.txt lookup keys** (`.data` layout is literally `\"prompts.txt\\0\" \"LIGHTNING_BOLT\\0\"`; on-disk Prompts.txt contains `@LIGHTNING_BOLT`). Only **430** such strings exist vs 1,250 cards in `Master.csv`. **`SENGIR_VAMPIRE` occurs 0 times in any binary.** There is **no per-card dispatch table** (zero runs of ≥8 consecutive `.text` pointers in `.data`; longest in-`.text` run is 17). By IAT-xref profiling, the claimed core `0x00430000–0x004C0000` makes **1,714 import calls** (865 USER32, 325 GDI32, 183 KERNEL32, 149 ADVAPI32 registry) — that is window/dialog/registry code. **Genuinely Win32-free compute is `0x00480000–0x004A0000`, `0x004D0000–0x004E0000`, `0x00510000–0x00520000` (~15 import calls total) ≈ 192–256 KB.** Find the real dispatch mechanism before sizing `src/rules`.\n\n### 5.2 Project layout\n\n```\nC:\\mtg\\                          # short path (insurance, not required)\n  orig\\                          # original binaries, gitignored, hash-verified\n  toolchain\\vc42\\                # vendored, hashed, C2.EXE patched\n  src\\\n    core\\                        # sid/glue, rules, NedCard  (shared SOURCE, built twice)\n    sidlib_shandalar\\            # D:\\NewMagic\\sources\\sidlib   — FORKED\n    sidlib_magic\\                # D:\\Newmagic\\multiplayer\\sidlib — FORKED\n    mps\\                         # MPS_CODE.ASM (one .obj, 6 links)\n    shandalar\\                   # world map, caves, city, ADVstrings driver, shell @0x4A0000\n    magic\\                       # shell/menu @0x450000, ManaLink packet layer @0x500000\n    dll\\{deckdll,drawcardlib,cardartlib,cdtools,statwin,magsnd,magvid,manalinkinterface}\\\n  link_order\\shandalar.txt magic.txt ...   # .obj order = .text layout. FIRST-CLASS INFRA.\n  tools\\                         # literal oracle, mps mask-hash check, patch manifest\n  reccmp-project.yml             # SHA-256 per target, all 15 modules\n  patches\\mok_1_3_2.yml          # 4 six-byte pokes + .mok payload\n```\n\n**`link_order.txt` is mandatory infrastructure.** `/Gy` was not used (functions are packed, no COMDAT scattering), so `/ORDER` is unavailable — `.text` layout is determined **solely by `.obj` order on the LINK command line**, and `.idata` by `.lib` order. Generate the LINK response file from a checked-in list. This is the PE analogue of splat's linker script. Its correctness is independently verifiable against the observed 16-byte-aligned `0xCC` gaps.\n\n**Import order is a free constraint — write the `.def` files and lib order in week 1.** Shandalar's 11 exports are ASCII-name-sorted: `CardIDFromType, CardInDeck, CardTypeFromID, Gold, Scards, SellPrice, SetCardInDeck, _OpponFace, _PlayerFace, _currentDeck, deck`. Import descriptor order: `KERNEL32, USER32, GDI32, comdlg32, ADVAPI32, SHELL32, WINMM, COMCTL32, MSVFW32, DrawCardLib, DECKDLL, CdTools, CardArtLib, MSVCRT` — **MSVCRT last**, which constrains where the CRT sits on the link line.\n\n### 5.3 Flag baseline — pin empirically, check into the repo\n\n**Already established from the binaries:**\n- `/Gf` and `/GF` **OFF** — string pooling disabled. `\"prompts.txt\"` occurs **249 separate times** in `.data` (3,400 literal occurrences, 2,366 unique, 338 duplicated; `promptsX1.txt` ×121, `advfac64.pic` ×34).\n- Literals live in **`.data`, not `.rdata`** (`.rdata` is only 746 bytes).\n- `/Gy` effectively **OFF**.\n- **`/Od`, not optimized.** Deck.exe's WinMain shows `jmp`-to-next-instruction, unshortened 6-byte `jcc`, stack reloads — and **Shandalar.exe at `0x401000` shows the identical signature. The whole game shipped unoptimized.** This is a major simplification: `/Od` removes most register-allocation ambiguity, and flag calibration from Deck.exe transfers to the shipping EXEs.\n- `/SUBSYSTEM:WINDOWS,4.0`, `/VERSION 0.0`, `NumberOfRvaAndSizes = 10`, no Rich header.\n- Per-target CRT: `/MD` (8 modules), `/MT` (Manalink.exe, Manalinkinterface.dll), `/MTd` (Cardartlib, Drawcardlib, Cdtools), MSVC 5.0 `/MTd` (Gcconn).\n\n**Still to determine by probe corpus (week 1, ~40 h):** `/Zp` struct packing, `/G3` vs `/G4` vs `/G5`, `/Gd` vs `/Gz` default calling convention, floating-point model, exact `/Ob` inline level. **Flag archaeology done wrong poisons thousands of downstream functions — do the probe corpus, do not infer opportunistically.**\n\n⚠ A static debug CRT implies `_DEBUG`, which conflicts with RelWithDebInfo's `/D NDEBUG`. The `/MTd` DLLs need optimized-code-plus-debug-CRT, which **no stock CMake config expresses.** Author per-target configs.\n\n### 5.4 The killer oracle — un-pooled string literals\n\n**This is the highest-value verification lever unique to this target and it costs almost nothing.** Because `/Gf` was off, **every literal occurrence is a distinct `.data` slot emitted in source order**. Build a tool that extracts the ordered literal sequence per `.data` region from both the original and the rebuild and diffs the sequences. This validates **TU segmentation, TU link order, and intra-TU statement order without matching a single instruction.** Run it in CI on every commit — orders of magnitude cheaper than assembly diffing, and it catches layout regressions instantly.\n\n### 5.5 Do data and layout BEFORE code\n\nBecause the EXEs are `RELOCS_STRIPPED`, every global address is baked into `.text` as an absolute immediate. **Relaying out `.data` after matching functions invalidates all of them.** `.data` VirtualSize `0x406DB0` (4.2 MB) vs RawSize `0x25E00` (155 KB) — MSVC 4.2 merged BSS into `.data` (`SizeOfUninitData = 0`). Data matching is **27× larger by virtual size than by raw size**, almost all uninitialised globals. Every global's size, alignment, and declaration order must be right or megabytes shift.\n\n**Define the core record array first.** Element stride **300 bytes**, row stride **45,300 = 151 elements/row**, base at or below `0x008CA274`. **841 of 2,234 functions (38%)** contain the ×300 lea-chain; **926 of 2,234** reference the `0x8C0000–0x8E0000` window. Hottest field displacements: `0x8CA278` (899 refs), `0x8CA2DC` (893), `0x8CA2A8` (887), `0x8CA2E4` (747), `0x8CA2E8` (679).\n\n**Cheapest witness: `0x00411D90`** — 148 bytes, 50 instructions, **0 callees, 188 callers**. It is a pure predicate: `if (tbl[a][b].+0x64 == -1) return 0; return (tbl[a][b].+0x00 & 0x800022) == 2;`. It yields stride, row stride, an int32 field with a `-1` sentinel, and bitfield bits `0x02`/`0x20`/`0x800000` for ~2% of the effort of `0x004BEA85`. Then `0x004C4263` (75 B, 133 callers), then `0x0046D8C1`.\n\nCross-check against `Cards.dat`, which is **fully solved structurally**: `{u32 numCards=999; u32 stringRegionBytes=188169}` + 999 × 152-byte records + NUL-terminated string blob. `8 + 999×152 + 188169 = 340,025` = exact filesize. Confirmed record fields (152 B = 38 LE int32): `+0` card id, `+4` name strOff, `+8` short name, `+16` colour enum {1 Black, 2 Blue, 3 Artifact, 4 Gold, 5 Green, 6 Land, 7 Red, 8 White}, `+20` type enum {1 Artifact … 8 Token}, `+28` type line, `+40..+48` mana cost array **indexed by the same colour enum** (slot 0 = generic — expect `card->mana[card->color]` idioms), `+76` art filename (`\"0000.wvl\"`), `+116` rule text, `+120` flavour, `+124` power, `+128` toughness. ~20 dwords remain unidentified (`dw3` bitfield `0x840`/`0xC00`/`0x801`, `dw6`, `dw8`, `dw9`, `dw16`, `dw33`, `dw34`) — these are the AI/rules fields; use `Rarity.csv`'s ~60 column headers (`\"AI Values\"`, `\"Dependencies\"`, `\"Class Value Modifiers\"`, `\"Persistant Attributes\"`, `\"Creature Abilities\"`, legend `\"1 has 2 gives 4 anti\"`) as the naming dictionary.\n\n**Universal join key to assert in tests:** `Cards.dat` record index == `.dck` `.N` value == `%04d` in the `.WVL` filename == sort order of `.cat` keys. One invariant validates four parsers. **Do not use `Master.csv`/`Info.csv` values as fixtures — `Cards.dat` disagrees with `Info.csv` on 127 of 999 names.**\n\n### 5.6 Matching workflow — make the inner loop sub-minute\n\n**Never make the agent rebuild and relink 1.5 MB to test one function.** Wall-clock per iteration dominates total project cost; a 30-second loop vs a 5-minute loop is the difference between 2 years and 10.\n\n- Primary: `reccmp-reccmp --verbose 0xADDR` for a single-function diff.\n- Stuck functions: decomp.me `win32`/`msvc4.2` scratch (exact compiler, zero setup, shareable URL).\n- Endgame: split → objdiff per-`.obj` (removes the link step entirely). Spike the delinker on Deck.exe before committing.\n\n### 5.7 Progress metric — three numbers, four states, never one headline\n\nAdopt **objdiff's report protobuf verbatim** (`objdiff-core/protos/report.proto`): one `Report` per target, one `ReportUnit` per TU, one `ReportItem` per function. Generate with `objdiff-cli report generate`; cross-check totals against reccmp's JSON.\n\n**Headline all three, always:**\n1. `matched_code_percent` (bytes) — the honest \"how done\"\n2. `matched_functions_percent` — the honest \"how much work left\"\n3. `complete_units / total_units` (~465 TUs across the two EXEs) — the stable human-legible one\n\n**Track four per-function states, not two:** `MATCHING` (byte-identical after reloc normalization), `EQUIVALENT` (compiles, believed correct, does not match), `STUB`, `UNKNOWN`. **Report `EQUIVALENT` separately and prominently — conflating it with `MATCHING` is how projects convince themselves they are 95% done at 60%.** `reccmp-stackcmp` exists to triage the `EQUIVALENT` band by stack-layout delta.\n\n### 5.8 CI on every commit (target <10 min)\n\n1. `reccmp-decomplint` — reject malformed/duplicate annotations, enforce ascending-address ordering per TU\n2. Full MSVC 4.2 build producing PDBs (`nmake && nmake`)\n3. `reccmp-reccmp --json current.json --silent` per target\n4. `reccmp-reccmp --diff base.json` — **fail the build on ANY regression from `MATCHING`** (isle's version only warns; ours must gate)\n5. `reccmp-verexp` (DLL exports) + `reccmp-datacmp` (globals)\n6. Whole-file SHA-256 with a per-target **allowlist of known-unmatched byte ranges that must only ever shrink**\n7. **The literal-sequence oracle**\n8. MPS_CODE masked-hash check (mask 115 relocs + rel32 at 215, assert `3836eee0…`)\n\nPublish `current.json` as the new baseline only on green.\n\n### 5.9 Double-entry check\n\nRequire every shared-core TU to match in **both** Shandalar.exe and Magic.exe before it is marked complete. A function that matches in one image but not the other has an address-dependent bug. This catches a whole class of errors for free and is not available to most decomp projects.\n\n---\n\n## 6. Phased plan\n\n| Phase | Entry criteria | Exit criteria | Agent-hours | Wall clock |\n|---|---|---|---|---|\n| **0 — Toolchain & pilot** | Repo exists; VC 4.2 media in hand | Full VC 4.2 tree vendored + hashed (`C2.EXE` MD5 verified, patched); `NMAKE`/`LIB`/`RC`/`CVTRES`/`LIBCMT.LIB` present; `reccmp-project.yml` with SHA-256 for all 15 modules; **Deck.exe's 114-byte WinMain byte-matches modulo relocations**; probe corpus resolves `/Zp`,`/G3-5`,`/Gd\\|/Gz`, FP model; CI green | **60–120** | 1–3 weeks |\n| **1 — Layout, oracles, MPS_CODE** | Phase 0 exit | `.data` map frozen (4.2 MB virtual) via `reccmp-datacmp` + GLOBAL annotations; `link_order.txt` per target committed and validated against the 262/170 `0xCC` gaps; literal-sequence oracle live in CI; **MPS_CODE matching in all 6 modules** (masked hash `3836eee0…`); 300-byte card struct typed and applied in Ghidra; `defs.h` types imported; `manalink.lds` names ported via signature matching; **one mega-function spiked** to learn switch-lowering | **300–500** | 4–8 weeks |\n| **2 — sidlib + small DLLs** | Phase 1 exit | Statwin.dll, Magsnd.dll, Magvid.dll at L1; Shandalar sidlib block **`0x00578C04–0x0057E81A`** (124 functions, ~24.4 KB, 76.5% 16-byte aligned) at L1; `reccmp-verexp` green on those DLLs; Facemaker.exe as second pilot (`SizeOfCode 0xA000`, relocs retained, no overlay, no `.mok`) | **400–700** | 2–3 months |\n| **3 — Bulk decompilation** | Phase 2 exit; ≥3 TUs complete at L1 in each EXE | ~2,330 EXE functions + Deckdll/Drawcardlib/Cardartlib at L1; Manalink.exe + Manalinkinterface at L0 minimum | **3,500–6,000** | 12–30 months |\n| **4 — Tail closure** | Phase 3 at ≥85% `MATCHING` | x86/MSVC decomp-permuter backend written (150–300 h of it); `EQUIVALENT` band driven down; whole-file L4 attempted on Statwin/Magsnd/Magvid; Mok patch manifest applied and verified | **800–1,500** | 4–9 months |\n\n**Totals.** Full L1 across the two EXEs + the main DLLs: **5,000–9,000 agent-hours, central ~6,500.** Reaching only isle's actual achieved bar (**L0**, the two EXEs): **3,000–4,500 agent-hours.**\n\n**Effort by function tier** (per-function, including iteration and review): Tier 0 CRT/library — matched by *linking the right `.lib`*, not writing source, ~100 h total, annotate `LIBRARY` and exclude from the denominator. Tier 1 trivial (≤64 B), ~1,000 functions × 0.25 h = **250 h**, expect 80–90% first-pass match under `/Od`. Tier 2 routine (64–512 B), ~1,200 × 0.75 h = **900 h**. Tier 3 hard (512–4,096 B), ~850 × 3 h = **2,550 h**. Tier 4 pathological (>4 KB, incl. the 20–100 KB dispatchers), ~170 × 12 h = **2,040 h**. Plus data/link layout ~400 h and tooling ~300 h.\n\n**Wall clock.** Single agent at ~6 productive hours/day: **3–4 years.** Four parallel agents on disjoint TU sets: **~9–15 months to L0, 2–3 years to broad L1** — *with human review as the binding constraint, not agent throughput.*\n\n**Calibration:** isle consumed 18 months and 41 contributors for ~2 MB of C++ to reach 100% implemented / 99.4–99.7% accuracy, and is still going at 38 months without byte-for-byte. Our target is ~1.66 MB of unique code, **easier per line** (C, `/Od`, no vtables/templates/mangling) but pursued to a **strictly higher bar with no community**.\n\n**Tiering — publish this as the definition of done:**\n- **Tier A** (attempt L4 whole-file): Statwin.dll, Magsnd.dll, Magvid.dll, Manalinkinterface.dll\n- **Tier B** (must reach L1 per-function): Shandalar.exe, Magic.exe, Deckdll.dll, Drawcardlib.dll, Cardartlib.dll\n- **Tier C** (best effort / L0): Manalink.exe, Cdtools.dll, Facemaker.exe ×2, Deck.exe *(L4 blocked by its overlay + `.rsrc`)*\n- **Tier D** (descope): **Gcconn.dll**\n\n---\n\n## 7. Top risks, ranked\n\n| # | Risk | Mitigation |\n|---|---|---|\n| **1** | **MSVC 4.2 codegen entropy — unrelated edits pseudo-randomly change codegen, ~5% of functions. isle's one unsolved blocker after 38 months.** | Keep TUs small (~8 functions / 5.6 KB each, matching the observed boundaries). Gate CI on `reccmp-reccmp --diff base.json` so drift is caught the commit it happens. **Accept this as the reason L4 whole-file is not promised for the big EXEs.** |\n| **2** | **No x86 backend for decomp-permuter — the standard closer for the final 5–10% does not exist.** | Budget **150–300 agent-hours in Phase 4** to fork it. The randomizer (C-source mutation) is architecture-neutral; only the scorer and compile-invocation layer are arch-specific. Alternative: accept a materially lower final match rate and say so up front. |\n| **3** | **`.data` layout error invalidates already-matched code.** No `.reloc` on either EXE; 4.2 MB of BSS merged into `.data`; a single wrong array dimension shifts megabytes. | **Freeze the data map in Phase 1, before bulk function matching.** `reccmp-datacmp` + `GLOBAL` annotations. Derive the core 300 B / 151-element table from `0x00411D90` (148 B, 0 callees) and cross-check against `Cards.dat`'s 152-byte record. |\n| **4** | **The engine is forked between the EXEs — no shared-static-library shortcut.** sidlib links from two different source trees; NedCard `Catalog.c`/`glue.c` differ in real codegen; only 24.2% of Shandalar's `.text` is object-level identical. | Plan **two compilations with independent verification**. Shared *sources* + per-target build configs (Shandalar's sidlib is assert-enabled with a debug palette window; Magic's is not). Budget ~70% of shared bytes for double work. Use the double-entry check (§5.9) to catch address-dependent bugs. |\n| **5** | **Toolchain acquisition and legal posture.** `itsmattkc/MSVC420` has no LICENSE and can be taken down mid-project; genuine media purchase is a human decision. | Buy genuine 2-CD VC 4.2 (cheap, confirmed available). **Vendor and hash a local copy on day one** regardless of source. Mirror at `madebr/MSVC420`. Verify with `C2.EXE` MD5 `dcd69f1dd28b02dd03dd7ed02984299a`. |\n| **6** | **Target-binary provenance is undocumented.** Your magic.exe (1,604,096), shandalar.exe (1,753,088), Manalink.exe (306,176) match **no published hash set** and are *smaller* than official ManaLink 1.3 (1,714,176 / 1,878,016 / 321,536). 8 of 14 modules are byte-identical to canonical; 6 differ. No third party can reproduce your baseline. | Either retarget to canonical ManaLink 1.3 (published MD5s in `rlerrr/specs.json`, archived at `archive.org/details/score54cd` as `mtg_13us.zip`) **or** explicitly document Mok 1.3.2 as the target and publish its hashes yourself. **Decide before writing `reccmp-project.yml`.** |\n| **7** | **~250–350 KB of static CRT outside the two EXEs.** Manalink.exe 24.8% of `.text`; Drawcardlib/Cardartlib/Cdtools 57.8%/65.2%/91.7% CRT-string spans. | Source the exact VC 4.2 `LIBCMT.LIB` and `LIBCMTD.LIB`; **annotate `LIBRARY` and exclude from the accuracy denominator**, as isle does. Treat CRT regions as prebuilt blobs, not recompiled source. Note isle **did** eventually byte-match statically-linked SmartHeap — this is solvable given the right lib, not a permanent wall. |\n| **8** | **Deck.exe cannot reach L4 with `cl` + `link` alone.** Unique 45-byte XOR-0x4F post-link build stamp at file offset `0x4000`; 4,363-byte `.rsrc` needing period `RC.EXE`+`CVTRES.EXE`+the original `.ico` (neither tool on this machine); 1,024-byte debug blob embedding a hardcoded path and PDB signature `0x3332DD16`; 4,294 bytes (82% of `.text`) of link-flag-dependent `0xCC` padding. | **Reframe the pilot: \"prove cl+link+reccmp plumbing on 114 bytes in an afternoon,\" not \"reach 100% on 5.6 KB in days.\"** Use a byte-exclusion allowlist. Promote **Facemaker.exe** as the second, more code-bearing pilot (`SizeOfCode 0xA000`, relocs retained, no overlay, no `.mok`). |\n| **9** | **Human review bandwidth is the true wall-clock bottleneck.** Parallel agents scale linearly only until review saturates. | Measure it in Phase 0. If review is a few hours/week, the parallel-agent strategy provides little benefit and the 3–4 year single-track estimate applies. Automate as much review as possible into CI (§5.8) so humans review *design*, not diffs. |\n| **10** | **`ShandalarMagic/Shandalar` license is unverified.** Matters if you copy `defs.h` struct definitions verbatim. | Verify before copying. If absent/restrictive, attribute and re-derive the layouts from the binary — slower but clean. Clone the repo now regardless (2 commits, frozen since 2021-09-03, no maintainer). |\n| **11** | **objdiff/delinker path is unproven for x86 PE.** The only precedent (`openblack/bw1-decomp`) sits at ~1.75% after substantial effort; `ghidra-delinker-extension`'s COFF/x86 exporter is far less exercised than its ELF/MIPS path. | **Do not depend on it.** reccmp covers the PE+PDB workflow with no splitting. Spike the delinker on Deck.exe in Phase 0 as a cheap experiment; if it fails, the L2 tier and the fast inner loop must be redesigned around per-function reccmp, which is slower per iteration. |\n\n---\n\n## 8. Open questions for the human\n\n1. **What is the definition of done — L0, L1, or L4?** These differ by a factor of 2–3 in total effort (3,000–4,500 vs 5,000–9,000 agent-hours) and the difference is invisible to anyone playing the game. **L4 across all binaries is not defensible on the evidence.** Recommend: L1 for Tier B, L4 attempted only on Tier A.\n\n2. **Which binaries are canonical — your Mok 1.3.2 install, or official ManaLink 1.3 US?** Yours match no published hash set and are smaller than official 1.3. Canonical 1.3 has published MD5s (`rlerrr/specs.json`) and is publicly archived (`archive.org/details/score54cd`), making your work independently verifiable. Yours is an undocumented variant. **This decision writes `reccmp-project.yml` and cannot be changed cheaply later.**\n\n3. **Toolchain posture: buy genuine VC 4.2 media, or use the `itsmattkc/MSVC420` mirror the way isle and CMR2Decomp do?** Both yield a byte-identical toolchain and the fingerprints to prove equivalence are known. Genuine media (2-CD Enterprise with key, confirmed on eBay for very little) is the clean-hands route.\n\n4. **Which of the 15 modules are in scope?** Recommended tiering is in §6. The specific sub-decisions: **is Gcconn.dll in or out** (needs MSVC 5.0, C++, 46 mangled exports, serves a network dead since 1999 — recommend out), and **is Manalink.exe in** (24.8% static CRT, adds a whole CRT-matching workstream).\n\n5. **Publishing and legal posture.** Public repo from commit one, or private? The established norm (sm64, Majora's Mask, Perfect Dark) is **source + extraction scripts, zero original assets** — no `Cards.dat`, `*.pic`, `*.spr`, `*.cat`, `*.dck`, `*.avi`, `*.wav`, card art. **Cheap to do at the start, very expensive to retrofit.** There is also a second IP layer here that most decomps lack: MTG card names, rules text, and art belong to Wizards of the Coast, entirely separate from the MicroProse engine code. **How much appetite is there for referencing card names even in test fixtures and comments?** (`ShandalarMagic/Shandalar` has openly redistributed the complete game including 23,045 card-art JPEGs since Sept 2021 without takedown — that is an enforcement datapoint, not a licence.)\n\n6. **Time budget and human review bandwidth per week.** The wall-clock estimate is dominated by review, not agent throughput. Four parallel agents get you ~9–15 months to L0; one agent gets you 3–4 years. **How many agent-hours are actually available, and how many hours per week can a human spend reviewing matched functions?**",
    "laneCount": 5,
    "claimsVerified": 24,
    "refuted": 9
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Recon"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "recon:prior-art",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a04951e3255d05f0f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785424790396,
      "queuedAt": 1785424790375,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Prior art and community ecosystem for MicroProse Magic: The…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785427609999,
      "tokens": 115288,
      "toolCalls": 70,
      "durationMs": 2819602,
      "resultPreview": "{\"topic\":\"Prior art and community ecosystem for MicroProse Magic: The Gathering (1997) / Shandalar / ManaLink — RE, modding, file formats, symbol maps, scene state 2026, and legal norms\",\"key_findings\":[{\"claim\":\"The single largest body of prior art is the ManaLink 3.0 source tree, dumped publicly at github.com/ShandalarMagic/Shandalar (94 stars). Its `src/` folder holds ~300 .c, 45 .asm, 107 .pl …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "recon:toolchain",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a1c66c54236c2cf66",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785424790405,
      "queuedAt": 1785424790375,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Period-correct toolchain strategy for byte-matching decompi…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785427923157,
      "tokens": 136562,
      "toolCalls": 83,
      "durationMs": 3132751,
      "resultPreview": "{\"topic\":\"Period-correct toolchain strategy for byte-matching decompilation of MicroProse Magic: The Gathering (1997) / Shandalar + ManaLink\",\"key_findings\":[{\"claim\":\"PROVEN, not inferred: the toolchain is Microsoft Visual C++ 4.2, and I have it running on this machine. I fetched CL.EXE/C1.EXE/C2.EXE/LINK.EXE/MSPDB41.DLL + MSVCRT.LIB/KERNEL32.LIB from the itsmattkc/MSVC420 drop into the scratchpa…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "recon:data-formats",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a226f1350593c6b07",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785424790412,
      "queuedAt": 1785424790376,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "MicroProse \"Magic: The Gathering\" (1997) / Shandalar + Mana…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433127222,
      "tokens": 238557,
      "toolCalls": 104,
      "durationMs": 8336810,
      "resultPreview": "{\"topic\":\"MicroProse \\\"Magic: The Gathering\\\" (1997) / Shandalar + ManaLink — game data file format inventory (evidence-backed)\",\"key_findings\":[{\"claim\":\"MPS_CODE is NOT (only) an anti-piracy stub — it contains the .pic image loader's magic dispatch. The bytes `66 3D 4D 30 74 08 66 3D 4D 31 74 02` (cmp ax,'M0' / je / cmp ax,'M1' / je) sit at exactly MPS_CODE+0x43 in four modules, immediately prec…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "recon:binary-structure",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "ac82fcac80dc35693",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785424790414,
      "queuedAt": 1785424790376,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Binary structure & subsystem decomposition of Shandalar.exe…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785427940001,
      "tokens": 168601,
      "toolCalls": 44,
      "durationMs": 3149587,
      "resultPreview": "{\"topic\":\"Binary structure & subsystem decomposition of Shandalar.exe / Magic.exe (MicroProse Magic: The Gathering, ManaLink 1.3.2 build)\",\"key_findings\":[{\"claim\":\"MPS_CODE is NOT copy protection, anti-debug, or a decryptor. It is a hand-written MASM LZW + RLE90 image decompressor — the codec behind the .pic/.pcx asset loader. Entry points: +0x000 chunk/tag stream reader (matches 16-bit tags 'M0'…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "recon:methodology",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "ae41968756fc5902b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785424790414,
      "queuedAt": 1785424790376,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Methodology, verification, progress-tracking and effort mod…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785426842478,
      "tokens": 91055,
      "toolCalls": 52,
      "durationMs": 2052063,
      "resultPreview": "{\"topic\":\"Methodology, verification, progress-tracking and effort model for a byte-matching decompilation of MicroProse Magic: The Gathering (1997) / Shandalar / ManaLink — Windows x86 PE, MSVC 4.2\",\"key_findings\":[{\"claim\":\"isle (LEGO Island, 1997, MSVC 4.20, x86 PE) is the direct methodological precedent and it did NOT achieve byte-for-byte. It reached 100% functional/instruction-matching decomp…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:toolchain:0",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "afa8f3795c0a76ce1",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127268,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The CONCLUSION is correct and I independently reproduced it…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433699792,
      "tokens": 58930,
      "toolCalls": 23,
      "durationMs": 572524,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"The CONCLUSION is correct and I independently reproduced it, then proved it far more robustly than the claim did. But the claim's stated PROOF is not a proof, its scope is overstated, and its IMPACT statement is wrong.\\n\\nWHAT HOLDS UP (independently re-verified, not taken at face value):\\n1. Toolchain files are genuinely MSVC 4.2. CL/C1/C2.EXE FileVersion=10.20 Produ…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:toolchain:1",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a1183492c2d73fd5c",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127270,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CORE CLAIM VERIFIED, AND UNDERSOLD. I re-ran objdump -x on …",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433769000,
      "tokens": 68228,
      "toolCalls": 24,
      "durationMs": 641730,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"CORE CLAIM VERIFIED, AND UNDERSOLD. I re-ran objdump -x on both files independently. All 12 stated hint values are exactly correct in Shandalar.exe: __getmainargs 0x58, __setusermatherr 0x81, _adjust_fdiv 0x9a, __p__commode 0x69, __p__fmode 0x6e, _initterm 0x10b, _except_handler3 0xc6, _controlfp 0xb3, __p__acmdln 0x67, _XcptFilter 0x48, __set_app_type 0x7f, _exit 0xc…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:toolchain:2",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4a92873d455eaa60",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127271,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Every testable version identifier in the claim reproduced e…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433652991,
      "tokens": 51813,
      "toolCalls": 33,
      "durationMs": 525720,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"Every testable version identifier in the claim reproduced exactly under independent re-testing, so the claim is not refuted. However two supporting elements are materially imprecise and must be corrected before this is used for multi-month planning.\\n\\nCONFIRMED INDEPENDENTLY:\\n1. LINK.EXE banner re-run from scratch: \\\"Microsoft (R) 32-Bit Incremental Linker Version 4…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:toolchain:3",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a5dbbe3ccd932c37f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127272,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The core claim survives adversarial re-testing and is if an…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433725881,
      "tokens": 58505,
      "toolCalls": 27,
      "durationMs": 598609,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"The core claim survives adversarial re-testing and is if anything understated. I re-ran everything from a clean tree rather than trusting the stated evidence.\\n\\nCONFIRMED INDEPENDENTLY:\\n1. Imports (objdump -x): CL.EXE, C1.EXE, C2.EXE, LINK.EXE each import MSVCRT40.dll + mspdb41.dll + KERNEL32.dll. MSPDB41.DLL itself imports only MSVCRT40.dll + KERNEL32.dll.\\n2. msvc…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:toolchain:4",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a937e0fd123ddff47",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127273,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Every concrete, checkable assertion in the claim survived i…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433492795,
      "tokens": 48175,
      "toolCalls": 15,
      "durationMs": 365522,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"Every concrete, checkable assertion in the claim survived independent verification against live GitHub (not the cached scratchpad copies), and the compiler-match premise was confirmed empirically against our own binaries.\\n\\nVERIFIED EXACT:\\n1. tools/patch_c2.py (live raw fetch) matches the claim byte-for-byte: ORIGINAL_C2_MD5 = \\\"dcd69f1dd28b02dd03dd7ed02984299a\\\"; C…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:toolchain:5",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af129570d7582b08d",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127274,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-fetched https://raw.githubusercontent.com/isledecomp/i…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433420955,
      "tokens": 42561,
      "toolCalls": 10,
      "durationMs": 293681,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I re-fetched https://raw.githubusercontent.com/isledecomp/isle/master/CMakeLists.txt live and diffed it against the saved scratchpad/isle_cmake.txt — they agree exactly. Every flag string in the claim is verbatim correct at lines 620-638:\\n\\n  620  set(CMAKE_CXX_FLAGS \\\"/W3 /GX /D \\\\\\\"WIN32\\\\\\\" /D \\\\\\\"_WINDOWS\\\\\\\"\\\")\\n  621  set(CMAKE_CXX_FLAGS_DEBUG \\\"/Gm /Zi /Od /D …"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:toolchain:6",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aad72952194eefc7a",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127274,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I independently re-ran the full survey rather than trusting…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434179789,
      "tokens": 80590,
      "toolCalls": 37,
      "durationMs": 1052514,
      "resultPreview": "{\"reasoning\":\"I independently re-ran the full survey rather than trusting the stated evidence.\\n\\nWHAT HELD UP:\\n- objdump -p import tables across all 15 PE files reproduce the 8/6 MSVCRT split exactly as claimed. Group 1 (Shandalar, Magic, Deck, Facemaker, Deckdll, Magsnd, Magvid, Statwin) import MSVCRT.dll and contain ZERO CRT __FILE__ strings (only app sources: Ai.c, Catalog.c, Pcxw.c, Fileio.c…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:toolchain:7",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aceb52b53e932b430",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127275,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-parsed both PEs with my own struct-level parser and di…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433855261,
      "tokens": 83042,
      "toolCalls": 37,
      "durationMs": 727986,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I re-parsed both PEs with my own struct-level parser and disassembled with Capstone rather than trusting the stated dump.\\n\\nCONFIRMED, exactly as claimed (byte for byte):\\n- Magic.exe (SHA-256 71ef20ea...61c6, 1,604,096 B) has a 7th section `.mok`, RVA 0x55A000 / VA 0x0095A000, vsize 0x1000, raw size 0x1000, raw ptr 0x186A00, characteristics 0xE0000040 (RWX + initiali…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:toolchain:8",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af789e7ceb78533c4",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127276,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Every literal piece of cited evidence reproduced exactly, b…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433728222,
      "tokens": 63359,
      "toolCalls": 26,
      "durationMs": 600946,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"Every literal piece of cited evidence reproduced exactly, but two of the claim's three assertions are wrong or materially overstated. The IMPACT (deprioritize/descope Gcconn.dll) is correct, so I did not mark it refuted — but the stated RATIONALE is the wrong model and would misguide planning.\\n\\nVERIFIED EXACTLY:\\n1. objdump -p Gcconn.dll -> MajorLinkerVersion 5 / Mi…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:toolchain:9",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a966e5399bb80cca2",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127276,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Every checkable technical particular in the claim survived …",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433612656,
      "tokens": 50930,
      "toolCalls": 21,
      "durationMs": 485380,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"Every checkable technical particular in the claim survived independent verification, and one sub-point I initially suspected was wrong turned out to be right when I read the source rather than a paraphrase.\\n\\nCONFIRMED INDEPENDENTLY:\\n1. README quote is verbatim. Live fetch of raw.githubusercontent.com/isledecomp/reccmp/master/README.md line 20 matches the saved scra…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:toolchain:10",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4223632efd40768a",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127277,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified each component against primary sources rather than…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433490508,
      "tokens": 41417,
      "toolCalls": 19,
      "durationMs": 363231,
      "resultPreview": "{\"reasoning\":\"Verified each component against primary sources rather than the stated evidence.\\n\\nCONFIRMED (verbatim):\\n1. objdiff README: \\\"objdiff compares two relocatable object files (`.o`)\\\" — correct, it does not diff whole executables.\\n2. decomp-permuter README: \\\"This tool supports MIPS (compiled by IDO, possibly GCC), PowerPC, and ARM32 assembly.\\\" — exact match, no x86/MSVC anywhere. T…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:toolchain:11",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7c7bc069281dbac8",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127278,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I attempted to refute this and failed on every substantive …",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433649159,
      "tokens": 65917,
      "toolCalls": 32,
      "durationMs": 521881,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I attempted to refute this and failed on every substantive point; it survived a stronger test than the one originally used to make it.\\n\\nINDEPENDENT RE-VERIFICATION (did not trust the saved evidence):\\n1. Re-downloaded backend/coreapp/compilers.py from GitHub main. `diff` against the recon agent's saved scratchpad/dm_compilers.py reports IDENTICAL (51,037 bytes). The…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:toolchain:12",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af22bc99e82baca52",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127278,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-ran every verifiable element from scratch rather than …",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433658926,
      "tokens": 52232,
      "toolCalls": 24,
      "durationMs": 531648,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I re-ran every verifiable element from scratch rather than trusting the stated evidence. Results:\\n\\nCONFIRMED EXACTLY (hard checks):\\n1. GitHub API for itsmattkc/MSVC420 (live now): size=30676 KB, stargazers=63, forks=3, created=2021-11-02T19:20:25Z, pushed=2021-11-02T19:24:28Z, archived=false, default_branch=master, license=NULL. Root listing contains bin/ include/ …"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:toolchain:13",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a2c8a45b5f62d533b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127279,
      "queuedAt": 1785433127235,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified each element against the binaries and against a re…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434032589,
      "tokens": 88833,
      "toolCalls": 42,
      "durationMs": 905310,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"Verified each element against the binaries and against a real VC 4.2 toolchain I found at C:\\\\vc42verify (LINK.EXE 4.20.6164, _MSC_VER==1020).\\n\\nCONFIRMED: Characteristics 0x10a (IMAGE_FILE_RELOCS_STRIPPED 0x1 NOT set) with a live .reloc section (0x14f bytes, 68 HIGHLOW fixups) — genuinely unlike Shandalar.exe/Magic.exe/Manalink.exe (all 0x10f = relocs stripped). Link…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:structure:14",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ace3d8c00359b5e92",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127279,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I independently disassembled MPS_CODE (Shandalar.exe, VA 0x…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434173858,
      "tokens": 115866,
      "toolCalls": 40,
      "durationMs": 1046578,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I independently disassembled MPS_CODE (Shandalar.exe, VA 0x0098a000, file offset 0x1a6600, vsize 0x4a8 = 1192 bytes, 378 instructions covering every byte) with capstone/pefile, and every load-bearing technical assertion in the claim reproduced byte-exactly.\\n\\nCONFIRMED BYTE-EXACT:\\n- Not DRM. I read all 1192 bytes. There is no CD check, no PEB/IsDebuggerPresent, no t…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "verify:structure:15",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a52cda565a0cbbb8a",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433127280,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-derived everything from the raw PE files with my own p…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785433963208,
      "tokens": 73403,
      "toolCalls": 27,
      "durationMs": 835927,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I re-derived everything from the raw PE files with my own parser (no reuse of the original script), plus capstone disassembly and independent .reloc-table parsing.\\n\\nREPRODUCED EXACTLY:\\n- Exactly six modules carry MPS_CODE: Deckdll.dll, Drawcardlib.dll, Facemaker.exe, Facemaker\\\\Facemaker.exe (\\\"Facemaker_sub\\\"), Magic.exe, Shandalar.exe. All VirtualSize 1192 / RawS…"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "verify:structure:16",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aaddcdff646f89f8d",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433420957,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The narrow structural evidence is exactly right and I repro…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434072098,
      "tokens": 87403,
      "toolCalls": 30,
      "durationMs": 651141,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"The narrow structural evidence is exactly right and I reproduced it independently, but the claim's framing and its planning-relevant IMPACT are materially wrong.\\n\\nVERIFIED INDEPENDENTLY (pefile + capstone, full-image 4-byte scan for each IAT slot VA, plus a self-built E8/E9 call graph):\\n- Shandalar.exe imports exactly 5 CdTools symbols: IsCDDrive (0x00987750), Check…"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "verify:structure:17",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a6dd02a03638224e3",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433490510,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I independently re-derived everything from `C:\\Users\\bo1026…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434399054,
      "tokens": 101949,
      "toolCalls": 33,
      "durationMs": 908544,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I independently re-derived everything from `C:\\\\Users\\\\bo1026\\\\Desktop\\\\RE Project\\\\Shandalar\\\\MtG_DotP_SotA\\\\Magic.exe` (SHA256 71EF20EA…61C6) with my own PE parser, pefile for imports, and capstone for disassembly. The claim's core mechanism is right, but three load-bearing structural assertions are false and would materially misdirect the project plan.\\n\\nCONFIRMED …"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "verify:structure:18",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a975c6b2da4627cde",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433492796,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I rebuilt the analysis from scratch (own PE parser, capston…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434476748,
      "tokens": 113092,
      "toolCalls": 46,
      "durationMs": 983951,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"I rebuilt the analysis from scratch (own PE parser, capstone disassembly, own matcher) rather than re-running theirs.\\n\\nREPRODUCED EXACTLY: non-thunk denominators 1,545,520 B / 2216 funcs (Shandalar) and 1,416,359 B / 1953 funcs (Magic); the 811 unique-normalized-hash anchors; and 987 = 811+176 with duplicate-hash groups resolved. Independent agreement on 811 to the …"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "verify:structure:19",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a080950af4ea10663",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433612659,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I independently re-derived everything with pefile + capston…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434232924,
      "tokens": 78078,
      "toolCalls": 26,
      "durationMs": 620265,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I independently re-derived everything with pefile + capstone (Python 3.12.10 at C:\\\\Users\\\\bo1026\\\\AppData\\\\Local\\\\Programs\\\\Python\\\\Python312\\\\python.exe). Scripts in ...\\\\scratchpad\\\\verify_crt\\\\ (imports/tail/scan/callers/dlls/bracket/ml_ep.py).\\n\\nWHAT CONFIRMED EXACTLY (the EXE-scoped structural core):\\n- Import counts are dead on: Shandalar.exe MSVCRT.dll = 89, M…"
    },
    {
      "type": "workflow_agent",
      "index": 26,
      "label": "verify:structure:20",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "afe1a3dcd9b3bf1da",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433649160,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-derived everything from Shandalar.exe directly (pefile…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434416765,
      "tokens": 105012,
      "toolCalls": 41,
      "durationMs": 767605,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I re-derived everything from Shandalar.exe directly (pefile + capstone linear sweep, plus an accurate instruction-boundary call census over all 2234 Ghidra function ranges), rather than trusting the stated evidence.\\n\\nWHAT REPRODUCES EXACTLY (the claim's raw metrics are all correct):\\n- 0x0056ca10 = 568 B, 361 unique callers (528 call sites). Its four indirect calls r…"
    },
    {
      "type": "workflow_agent",
      "index": 27,
      "label": "verify:structure:21",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a5e6a269aad85246a",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433652992,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I re-derived everything from the raw PEs with my own parser…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434617495,
      "tokens": 118509,
      "toolCalls": 53,
      "durationMs": 964503,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I re-derived everything from the raw PEs with my own parser (no reliance on stated evidence). Results split sharply.\\n\\nWHAT SURVIVED (verified, precise):\\n1. All 13 named anchor addresses are EXACT function starts. I built an independent function census from E8 rel32 call targets; every claimed address (0042d9e0, 0042f208, 004c357e, 004c80d8, 004cdd70, 00559cc3, 00566…"
    },
    {
      "type": "workflow_agent",
      "index": 28,
      "label": "verify:structure:22",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "acd9ab32438956bcb",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433658927,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I rebuilt the analysis from the binaries with my own toolin…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434514135,
      "tokens": 115625,
      "toolCalls": 31,
      "durationMs": 855208,
      "resultPreview": "{\"refuted\":true,\"reasoning\":\"I rebuilt the analysis from the binaries with my own tooling (pefile + capstone, Ghidra's function boundary CSVs at C:\\\\Users\\\\bo1026\\\\Desktop\\\\RE Project\\\\mtg-re\\\\reports\\\\ghidra\\\\{Magic,Shandalar}.exe.functions.csv), normalizing away image-range immediates/branch targets so layout drift could not masquerade as divergence, then matching functions with unique-hash seed…"
    },
    {
      "type": "workflow_agent",
      "index": 29,
      "label": "verify:methodology:23",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a723109f640a22ef8",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785433699795,
      "queuedAt": 1785433127236,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED VERBATIM (primary sources re-fetched, not taken o…",
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785434042195,
      "tokens": 51778,
      "toolCalls": 26,
      "durationMs": 342400,
      "resultPreview": "{\"refuted\":false,\"reasoning\":\"CONFIRMED VERBATIM (primary sources re-fetched, not taken on faith):\\n\\n1. Byte-for-byte NOT achieved — CONFIRMED, and still current. Live raw master README.md line 15: \\\"Due to various complexities with regard to the compiler, these binaries are not a byte-for-byte match of the original executables. We remain hopeful that this can be resolved at some point.\\\" README'…"
    },
    {
      "type": "workflow_agent",
      "index": 30,
      "label": "synthesize:brief",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "a683a1fcd1c4c7060",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785434617500,
      "queuedAt": 1785434617499,
      "attempt": 1,
      "promptPreview": "PROJECT CONTEXT (established facts — do not re-derive, build on these):\n\nTarget: MicroProse \"Magic: The Gathering\" (1997) PC game, the Shandalar campaign, plus the\nManaLink multiplayer module. Install dir: c:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA\nThis specific install is \"Duels of the Planeswalkers\"-era files patched by Mok's UNOFFICIAL\nManaLink update v1.3.2 (2001).\n\nGOAL: near …",
      "lastProgressAt": 1785435000530,
      "tokens": 135932,
      "toolCalls": 0,
      "durationMs": 383030,
      "resultPreview": "# MTG (MicroProse, 1997) — Shandalar / ManaLink: Byte-Matching Decompilation Recon Brief\n\n**Date:** 2026-07-30 · **Target install:** `C:\\Users\\bo1026\\Desktop\\RE Project\\Shandalar\\MtG_DotP_SotA` · **15 PE modules** (two distinct Facemaker builds)\n\n---\n\n## 1. Verdict on feasibility\n\n**Near-100% byte-for-byte across the target set is not realistic. Near-100% *per-function* byte equality after relocat…"
    }
  ],
  "totalTokens": 2701242,
  "totalToolCalls": 1076
}